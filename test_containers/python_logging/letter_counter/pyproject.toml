[project]
name = "letter-counter"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "duui-logging",
    "fastapi>=0.141.1",
    "pydantic>=2.13.4",
    "uvicorn>=0.52.1",
]

[tool.uv.sources]
duui-logging = { git = "https://github.com/texttechnologylab/DUUIlogger" }
